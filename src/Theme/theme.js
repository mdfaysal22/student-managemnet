import { createTheme } from "@mui/material";

const theme = createTheme({
    palette: {
      primary: {
        light: '#FFF6F5',
        main: "#F33823"
      },
      secondary: {
        main: '#FFFFFF'
      }
    }
  })


  export default theme;